name: Java CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Clear stale aesh-readline cache
        run: rm -rf ~/.m2/repository/org/aesh/terminal-api ~/.m2/repository/org/aesh/readline-api ~/.m2/repository/org/aesh/readline ~/.m2/repository/org/aesh/readline-all
      - name: Build with Maven
        run: mvn clean install
