## Setup Commands
- To build the project: `mvn clean install`
- To run all tests: `mvn test`
- To run a single test class: `mvn test -Dtest=MySpecificTestClass`

## Project Knowledge
- Key Technologies: Java 17, Maven
- All new code must include meaningful unit tests
